<resources>
    <!-- Base application theme. -->
    <style name="BaseAppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme" parent="BaseAppTheme">
        <!-- 状态栏图标字体颜色为黑色-->
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:windowLightStatusBar">true</item>

        <item name="colorControlNormal">@color/textColorThird</item>
        <item name="colorControlActivated">@color/main_select_color</item>
        <item name="colorControlHighlight">@color/main_select_color</item>
    </style>

    <style name="ToolbarTextAppearance">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">#000</item>
    </style>

    <style name="MyTabLayoutText" parent="TextAppearance.Design.Tab">

            <item name="android:textSize">13sp</item>

    </style>
</resources>